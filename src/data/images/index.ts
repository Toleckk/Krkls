import * as devicesImages from './devices'
import * as weaponsImages from './weapons'
import * as shipsImages from './ships'

export const devices: Record<string, string> = devicesImages
export const weapons: Record<string, string> = weaponsImages
export const ships: Record<string, string> = shipsImages
